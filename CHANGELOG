v0.1.0
- nix
- LazyVim

v0.1.1
- install & uninstall
- fish, starship, zoxide
- silent mode

v0.2.0
- 占有前询问
- preset

v0.2.1
- alias rm

v0.3.0
- switch to event
- compile

v0.3.1
- polish README
- add lazygit, bat, atuin

v0.3.2
restructure for plugins

## NOW 
- conf everything from zero
- use custom plugin and profile
- generate preset.fish
- 提拉米苏

## lvl0
- 用这个项目写课本，追踪你的学习路线？（怎么听着像[隔壁项目](https://github.com/AmourAmer/xyloq)干的事情？）
- 确定结构并自动make+所需特性齐全

## lvl1
required:
- ？
  - use emoji
  - 订阅列表关键字，比如这个项目只支持nvim，需要emacs可以先去推荐列表看看或者自己找一些别的来订阅，但是用到的时候才下具体的库
  - 你想用电脑做什么事情，并不是用来给你推广告的！profile/options？聊天，上网，打游戏，录屏，开发，办公，学习，每天长时间手动使用，服务器，音乐，画画，工程
    - 去直播间
- learn how to ask. search 1st
  - github issue https://github.com/Anduin2017/HowToCook/issues/1317
- tedious mode, show conf diff
- show examples of grep args
- fish completions
- 长耗时/需要学习的指令给出prompt
- 用户数据
  - 禁用已学会的指令
  - 我喜欢/讨厌这个prompt/sth else
- 抄自发展规划文件的，唯一一行我看不懂的：安装方式的database
- 文件管理，自动检测可删除文件或归档
- 小指令
  - 自定义备忘notes，unicode之类的
  - add license, interactively, and postponed
  - wallpaper
  - kopia chezmoi 备份！/快照！
  - font config lxgw
  - screenshots
  - 文件同步，用syncthing什么的开源产品
  - alias git to gji(or 饭桶) with wonderful template
  - journal 
  - auto install input method
  - use pinyin/other scheme, rime-ice and double-pinyin conflicts(wait, as you can subscribe to nur, you can also use lr(local repo))
  - caps2esc


other:
- shasum rawprofile
- 高效简洁模式，或者温柔模式，搞笑模式（听着也像隔壁的）
- info pager? interact?
- searchable tags
- auto git commit when no change in 5min
- insane 3-stage rm 
- execute demo cmds in sandbox(maybe bwrap?)
- nix iso with pre-installed cmdstorm
- 法律模板
- an app named what to do
- 强制干活，不然不跑指令？
- 终端聊天？
- patch conf.nix
- poems
- undo profile(with git?)
- 切换kde主题
- firefox plugins auto dark
- run github repo, with sandbox and auto-detect run cmds
- other lang? maybe prefix with a v or ,?
- tui，informative
- draft area, where you can create repos and we auto git it after several days
- fancy effect on clearing todo
- file format convert, fffmpeg, pandoc
- where did I learn them
- import bash and fish history to atuin? at least ask
- fucking cuda in nix, also python
- 密码管理
- show tips when there're longtime cmds, like updating or downloading, compiling
- yakuake
- tmux
- fucking find
- replace excel?!
- 也许需要一些玩意儿来处理yaml toml什么的


## lvl2
- add comments
- version related?(what's this?)
- backup, add time info something when ... conf file
- set show passwd * or not, and indicate
- gradually learn and config nvim
- abbr
- tldr auto update(should be configurable?)
- git branch rebase
- auto git profile change(what does this mean?)
- 抱怨没有readme的git repo
- 配置文件没有默认，必须写出，提示所有可选项，不写就不干，自动生成管理（但愿能做到）
- 查看已手动安装的程序
- what if profile.lua fails
- poems
- 字体 已知问题 对比（？）
- kitty背景图或透明
- show patch upon generating conf
- we use unstable unfree(?)
- auto gitignore
- lazygit delta difft(slow)
- steam install game into global position with correct permission(maybe game grp?)
- switch between profiles, that's just profile.raw.lua and local/share data?
- permission issue?
- record screen(customize with previews)
- todo 故事的力量 taskwarrior website?
- make functions return more meaningful and exact than simply 0
- 渐进学习typst
- map K to translate
- hyprland美化，预设与教程
- ranger(add the ctrl+d drop)
- fzf
- slidev
- use gpl proj and violate
- read user yes/no/all/never(how does chezmoi capture my input without return?) and show diff on conf file
- free chat user app
- obs virtual camera
- free as in freedom
- add OIer profile, with gdb
- add license easily and interactively, able to postpone that. Should search for it first, or write it in gpl
- smart alarm to remind you to stand up, randomly generate interval
- play the music!
- git-lfs
- notify on pkgs/ change
- if profile.lua is much too big, eval and replace it.
- add 提拉米苏 to todo
- remind you to subscribe to some health channel? I won't recommend you any.
- what's the difference between local share and state?
- global theme changes with time, using, forgot.
- print lua table, used in back_link and shrinking profile. output lua table as yml, in lazygit
- install all pkgs in the profile
- change the spell.fish to format "cmd(alias)"&"cmd(run)"?
- show files to write and prompt
- pokemon 
- 可用字体
- ignore list
- user config patch
- texlive
- 查看用量
- 格式化u盘，免责声明，强提醒
- community plugin safety
- show user confs when ask them whether to replace, and backup
- why my atuin only searches cmds in one day?! WTF?!
- zathura and synctex
- 学习咒语也要告诉用户它是在哪定义怎么实现的
- self host typing website

## other

- front end of fcitx in fish?
- emacs support? recruit

等项目相对成熟了，某一天当所有的PR和issue都被处理完后，我就归档这个repo

## NOTICE
- 配置文件没有默认，必须写出，提示所有可选项，不写就不干，自动生成管理（但愿能做到）
- 保留关键字：@（作插件前缀？）
- 不要用$1这种，可能会被翻译成其他语言
- 注意指令依赖，如systemd
- 文章求精不求量
