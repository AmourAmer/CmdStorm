function 出没风波里 # 一次性用品，小小带个路而已
    echo 还没写，运行此函数会source必要的文件，还会检测并问你要不要自动source，还会给一些简要的使用提示，你可以每次都输出没风波里以查看这些说明，如果你记住了可以直接source
    echo 对了，fish里按右可以自动填充
    read -p 警告，将要移除~/.config/fish/functions/出没风波里.fish
    rm ~/.config/fish/functions/出没风波里.fish
end
