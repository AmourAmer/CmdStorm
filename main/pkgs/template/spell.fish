if test "$cmd" = cmd_storm_template
    echo "Listening on some commands and do something before that"
    # echo "M.pkg = M.pkg or {}" 添加追踪信息再写入配置文件，这几个中文字会在编译的时候被换掉
    # emit CmdStorm_profile_changed
end
